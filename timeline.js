let amp;

function setup(){
    sketchWidth = document.getElementById("timeline").offsetWidth;
    sketchHeight = document.getElementById("timeline").offsetHeight;
    timeline = createCanvas(710, 200);
    timeline = createCanvas(sketchWidth, sketchHeight);
    timeline.parent("timeline");
    timeline.position(0, 0);
    

}

function draw(){


}
